<!doctype html><html lang="ja"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>納品書</title><style>body{font-family:"Noto Sans JP",Meiryo,sans-serif;background:#f3f6fa;color:#172033;margin:0;padding:18px}.panel{background:#fff;border:1px solid #d9e2ee;border-radius:8px;padding:14px;margin-bottom:12px}.row{display:grid;grid-template-columns:1fr 180px auto;gap:8px}input,select,textarea,button{font:inherit;padding:8px;border:1px solid #cbd7e6;border-radius:6px}button,.btn{background:#0b6ff6;color:#fff;text-decoration:none;border-color:#0b6ff6;font-weight:700}.table{width:100%;border-collapse:collapse}.table th,.table td{border-bottom:1px solid #edf1f6;padding:8px;text-align:left;font-size:13px}.msg{background:#e9f8ef;color:#137333;padding:10px;border-radius:6px;margin-bottom:12px}</style></head><body>
@if(session('status'))<div class="msg">{{ session('status') }}</div>@endif
@if($errors->any())<div class="msg" style="background:#fff1f0;color:#b42318">@foreach($errors->all() as $error)<div>{{ $error }}</div>@endforeach</div>@endif
<div class="panel"><h1>納品書作成</h1><form method="post" action="{{ route('retail.deliveries.store') }}" target="_blank">@csrf<div class="row"><select name="retail_sale_id" required><option value="">販売を選択</option>@foreach($sales as $sale)<option value="{{ $sale->id }}">{{ $sale->sale_no }} / {{ $sale->customer?->name }} / ¥{{ number_format((float)$sale->total_amount) }}</option>@endforeach</select><input type="date" name="delivery_date" value="{{ now()->toDateString() }}" required><button>納品書作成</button></div><p><textarea name="note" style="width:100%" placeholder="備考"></textarea></p></form></div>
<div class="panel"><h2>納品書一覧</h2><table class="table"><thead><tr><th>番号</th><th>日付</th><th>顧客</th><th>販売</th><th>状態</th><th></th></tr></thead><tbody>@foreach($deliveries as $delivery)<tr><td>{{ $delivery->delivery_no }}</td><td>{{ $delivery->delivery_date->format('Y-m-d') }}</td><td>{{ $delivery->customer?->name }}</td><td>{{ $delivery->sale?->sale_no }}</td><td>{{ $delivery->status === 'cancelled' ? '取消済み' : '発行済み' }}</td><td><a href="{{ route('retail.deliveries.show',$delivery) }}" target="_blank" rel="noopener">表示</a></td></tr>@endforeach</tbody></table>{{ $deliveries->links() }}</div>
<p><a href="{{ route('retail.pos') }}">販売へ戻る</a></p></body></html>

