# G5 B2 受入検証記録（2026-09-07）

## 範囲

承認済みのA完全昇格後、B2の書込みを伴わない基礎受入検証を実施した。B1には接続・変更を行っていない。

## 構成確認

- A・B2のGitコミットはともに `1953118d4210a94315d05779c6e87e43439c4124`。
- B2のDB `aikura` はAダンプから切替済み。公開テーブル数は102、`migrations` は111件でAと一致。
- `aikura_nginx`、`aikura_app`、`aikura_postgres`、`aikura_redis` は稼働中。PostgreSQLとRedisはhealthy。
- B2のトップページはHTTP 200。`/sales-orders`、`/inventory`、`/billing`、`/tax` はすべて認証前にログイン画面へ302で誘導されることを確認。

## データ照合

以下はAとB2で一致した。すべて `SELECT` による読取り確認である。

| 指標 | 件数 |
| --- | ---: |
| 未検証制約 | 0 |
| production_lots | 322 |
| stock_movements | 721 |
| stock_lot_monthly_balances | 169 |
| sales_orders | 0 |
| shipment_headers | 443 |
| invoice_headers | 62 |
| liquor_tax_monthly_filings | 3 |
| consumption_tax_monthly_filings | 0 |

## 認証済み画面・帳票確認

2026-09-08にB2で管理者権限の既存ブラウザセッションを用い、データを書き換えない画面・帳票表示を確認した。パスワードの送信・保存は行っていない。

- ロット現在庫、請求・入金、酒税申告、ロット月次在庫残高の各画面を表示できた。
- 在庫移動721件の対象期間は2026-07-01から2026-07-31である。`/inventory/movements/print?year=2026&month=7` は722行（見出し1行、明細721行）の帳票として表示され、ブラウザエラーは0件だった。
- `/inventory/lot-stock-as-of/print` はロット在庫一覧を表示した。
- 請求画面は2026-07指定で表示でき、作成・計算は実行していない。
- 既存の酒税申告は、2026年5月が `draft`、2026年6月が `confirmed`、2026年7月が `draft`。申告作成・確認・提出・設定変更は実行していない。
- 2026年7月のロット月次在庫残高は169件すべて `confirmed` であることを確認した。承認済みの残高再計算は、確定済み月を変更する操作となるため実行していない。実装も `CreateStockMonthlyBalanceDraftService` で `confirmed` / `closed` の月を再計算不可として保護している。

## ロールバック資産

- 旧B2 DB: `aikura_before_a_promotion_20260907_230411`
- 旧B2添付データ: `storage/app.before-a-promotion-20260907-230411`
- B2直前バックアップ: `C:\aikura-backups\b2-before-a-promotion-20260907-221500`
- A移行ステージング: `C:\aikura-staging\a-b2-promotion-20260907-230411`

上記を削除または変更するには、受入承認後に個別の明示承認を要する。

## 未完了の受入項目

- 権限を持つ検証用利用者による、受注・ロット引当・在庫照会の画面確認。
- 検証データまたはAで再現済みのシナリオによる、帳票の表示・印刷結果確認。
- 未確定の検証専用期間を指定し、月次在庫、売掛、酒税、消費税の作成・確定・再表示を利用者承認のもとで確認。確定済み月を変更する場合は、月次解除・訂正・再集計の対象、影響、復元手順について別途個別承認を要する。
- メール、印刷、外部API、キュー、スケジューラが検証中に外部へ影響しない設定であることの確認。
- B1-A照合およびB1の最終差分取込は、B1を正本として継続する別工程であり、本記録では未実施。

この記録はB2の本番リリース承認ではない。B2の実入力、外部連携の有効化、旧資産の削除、B1停止は行わない。
