# A-B2 完全移行の実行条件

この文書は、B1からAへの検証とAからB2への完全昇格を安全に進めるための必須ゲートである。前のゲートが未完了の間、次のゲートの破壊的操作を実行してはならない。

本番開始前はB1を日々の取引・ロット・入出庫・入金の唯一の業務正本とする。AはB1の初回基準取込後、定期差分を取り込み、Aで月次処理・帳票・業務検証を行う環境とする。Aへの実業務の手入力、外部連携による登録は行わない。B2は検証済みのA全体を完全昇格して検証する環境とし、B2だけに恒久データを作らない。

## G1. ロット在庫スキーマ

- 在庫の正本は `stock_movements`、月次確定値は `stock_lot_monthly_balances` である。
- `production_lots.product_id`、`stock_movements.product_id`、商品別在庫予約、商品別月次在庫は存在しない。
- 商品・税務情報を取引履歴に残す場合も、在庫残高の集計キーに使用しない。
- ロット在庫スキーマ、月次作成、月次確定、月次API、月次ジョブの自動テストが成功する。

## G2. 在庫業務フロー

- 入庫、出荷、出荷取消、棚卸調整、販売外出入、返品、ロット引当、品出から出荷までをロット付き在庫移動で検証する。
- 旧商品別残高を参照するコード・Seeder・帳票を廃止またはロット集計へ置換する。
- 各フローで、物理在庫、引当数量、利用可能数量、月次確定後の変更禁止を照合する。

## G3. B1-A データ検証

- B1 Access原本は読取専用で抽出し、原本SHA-256、抽出行数、取込バッチIDを保存する。
- 初回だけ承認済みの基準バッチをAへ取り込み、以後は固定した基準バッチとの差分を `Plan -> CSV確認 -> Apply` の順で取り込む。Aの月次確定値、帳票出力、監査記録を白紙化しない。
- 取引の `changed` / `deleted`、または月次確定済み期間へ影響する差分は自動適用しない。影響範囲、月次解除・訂正・再集計手順を示し、個別承認を得る。
- 差分適用前に、B1原本ハッシュ、比較元・対象バッチ、差分CSV、件数、対象期間、Aバックアップと復元手順、承認をそろえる。
- Aへ投入する前にロット対応不能、単位不一致、数量・金額・税額差異を隔離し、業務在庫へ自動投入しない。
- AでB1との伝票件数、数量、金額、酒税、開始在庫を照合し、利用者が結果を承認する。

## G4. A リリース候補

- 全自動テストが成功する。失敗を例外にする場合は、対象、影響、期限、承認者を記録する。
- AのDBダンプ、アプリ成果物、環境設定の識別子、SHA-256、復元手順を同じリリース記録に固定する。
- 外部メール、API、キュー、スケジューラを止めた状態で復元検証を行う。

## G5. B2 完全昇格

- B2のPostgreSQL、添付ファイル、設定のバックアップと復元確認を完了する。
- 明示承認後にのみ、固定済みのA成果物をB2へ配置し、Aの論理DBダンプを復元する。
- B2でスキーマ、在庫、業務画面、帳票、税務、監査ログ、バックアップを受入検証する。
- B1の停止と本番切替は、B2の受入承認後にB1入力を停止し、停止直後の最終差分をAへ取り込み、月次・在庫・売掛・税額を照合してから実施する。

## 現在の判定

G1のスキーマ検証は完了した。G2は2026-09-07にロット在庫、入出庫、出荷、出荷取消、棚卸調整、販売外出入、返品、ロット引当、品出、デモSeeder、出荷APIを対象とする70テスト・378アサーションが成功し、完了とする。G3のバッチ39→40を対象とするB1-A差分移行サイクルは2026-09-08に完了した。対象の出荷・入金照合不一致0件、確定請求275円訂正、対象売掛再集計、商品詳細名称の対応ロット27件上書きを確認した。開始前差分61件と上書き先のない商品詳細名称44件はB1原値保全として完了分類し、既存ロット・在庫移動・確定月次は維持した。8月月次処理は利用者がAで行う後続業務として移行時作業から除外する。税務確認は原値と確認フラグを残す業務確認項目であり、今回の差分移行サイクルの未完了には数えない。詳細は `docs/g3-batch40-delta-review-20260908.md` を参照する。最終切替時はB1停止後に別バッチで最終差分の取得・照合を行う。G4の事前確認は2026-09-06に実施したが、全自動テスト失敗、未コミット変更のため停止した。詳細は `docs/g3-b1-a-validation-20260906.md` と `docs/g4-release-candidate-precheck-20260906.md` を参照する。

G5のB2退避は2026-09-07に完了した。B2上の `C:\aikura-backups\b2-before-a-promotion-20260907-221500` に、`aikura` の論理ダンプ、`storage\app`、`.env`、`docker-compose.yml` を保存した。ダンプは `668191` バイト、SHA-256は `98D68FC08952007845878A64363E7F9700109A5ACA83C4A6D48141CB5974D6A0` である。業務DB `aikura`（公開テーブル79個）を変更せず、検証専用DBへ復元して公開テーブル数が79個で一致することを確認した。検証専用DBとコンテナ内の一時ダンプは確認後に削除した。

G4のAリリース候補バックアップは2026-09-07に作成した。DBダンプは `storage/app/backups/aikura-a-b2-candidate-20260907-230411.dump`、`277763044` バイト、SHA-256は `FFE92175C718925EC2310FC891B965627FA83814C029E3E3BE1671FEC8D46247` である。添付データアーカイブは `storage/app/backups/aikura-a-b2-candidate-storage-20260907-230411.zip`、`477872391` バイト、SHA-256は `FE0B60712EC204E952980A414459873BE8B8D431CFAE07D5C26C424A8D444186` である。既存バックアップを二重に含めないため、添付データアーカイブから `storage/app/backups` は除外した。DBダンプはAの検証専用DBへ復元し、公開テーブル数が業務DBと同じ102個であることを確認した。検証専用DBとコンテナ内の一時ダンプは確認後に削除した。

G5の初回完全昇格は2026-09-07に明示承認のもと実施した。AダンプをB2の候補DBへ復元し、公開テーブル数102個を確認後、候補DBを `aikura` へ名称切替した。旧B2 DBは削除せず `aikura_before_a_promotion_20260907_230411` として保持する。添付データも旧 `storage/app` を `storage/app.before-a-promotion-20260907-230411` へ退避してから、Aの535ファイル・`1797132646` バイトを新しい `storage/app` として配置した。`aikura_nginx` と `aikura_app` を再起動し、全コンテナ稼働、トップページHTTP 200、A・B2とも `migrations` 111件を確認した。B2の `.env`、`docker-compose.yml`、Gitコミット `1953118d4210a94315d05779c6e87e43439c4124` は変更していない。

B2ステージング領域 `C:\aikura-staging\a-b2-promotion-20260907-230411` には、検証済みのAダンプ、添付データアーカイブ、展開済みデータを残している。これは受入検証・ロールバック期間中の保全資産であり、削除は受入承認後に個別承認を得て実施する。

この時点でも、B1への書込み、B2への追加のコード配置・DB復元・設定変更・コンテナ停止再起動、旧B2 DB・旧添付データ・ステージング資産の削除は、個別の明示承認がない限り実行禁止とする。B2の本番リリースは、G5の業務受入検証を完了して利用者が承認するまで実施しない。
