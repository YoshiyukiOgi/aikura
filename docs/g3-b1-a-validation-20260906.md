# G3 B1-A データ検証記録（2026-09-06）

## 対象と保護状態

- B1原本: `C:\Documents and Settings\itaro\Itaro-xp.accdb`。読取専用で抽出し、B1への書込みは行っていない。
- 原本SHA-256: `B18CA5BD31EB6A8EF64DD5DE658B605B0BE953F66A516329A00E51BFDBB33D5C`
- 原本サイズ: `84,545,536` bytes。
- 抽出パッケージ: `storage/app/access-migrations/20260906-224707-b18ca5bd31eb`。原本のローカル一時コピーは検証後に削除済みである。
- B2への接続、コード配置、DB変更、設定変更は行っていない。

## バッチ40の機械検証

- Aの移行バッチID: `40`。
- Access抽出: 45テーブル、483,147行。
- Aステージング: 28テーブル、455,976行。ステージング対象外の17テーブルは設定済みの取込対象外であり、業務データへは投入していない。
- 行数不一致、伝票ヘッダー欠落、取引先欠落、商品欠落のエラーはすべて0件。
- バッチ39との差分は689行である。内訳は、出荷伝票・商品485行、出荷伝票・取引先141行、入金61行、伝票外在庫出入1行、取引先マスター1行である。

## 隔離した警告

警告は業務テーブルへ反映せず、すべて `access_migration_issues` に原本テーブル、ステージング行番号、原本キーを付けて保存した。未解決警告は2,136件である。

| コード | 件数 | 扱い |
| --- | ---: | --- |
| `zero_quantity` | 65 | 0数量明細として原本を保持し、移行可否を業務確認する。 |
| `negative_quantity` | 1,370 | 返品・訂正などの旧方式の表現を伝票単位で分類し、ロット在庫へ自動投入しない。 |
| `future_shipment_date` | 5 | 基準日と伝票日を確認するまで移行対象から除外する。 |
| `shipment_amount_mismatch` | 10 | ヘッダー金額と明細金額を照合し、採用値を承認する。 |
| `liquor_tax_classification_flag_mismatch` | 686 | 酒税区分を候補値とし、原本フラグを保存して税務確認する。 |

## 一次分類

ステージング行とAccessヘッダーを照合した結果であり、業務反映の承認ではない。

| 対象 | 一次分類 | 件数 | 扱い |
| --- | --- | ---: | --- |
| 負数明細 | `戻入取引=true` | 1,369 | 返品候補。元ロットがB1に存在しないため、ロット在庫へ自動反映しない。 |
| 負数明細 | `戻入取引=false` | 1 | 伝票`36989`。金額差異もあるため、業務確認まで隔離する。 |
| 0数量明細 | 通常 | 54 | 在庫移動を作らず、履歴として保持するかを確認する。 |
| 0数量明細 | 酒税未納 | 9 | 税務確認まで隔離する。 |
| 0数量明細 | 酒税未納かつ輸出 | 2 | 税務確認まで隔離する。 |
| 将来日付 | 2026-09-07 | 5 | 抽出日後の伝票`93655`、`93656`、`93657`、`93658`、`93662`。最終B1差分に含めて再照合するまで反映しない。 |
| 金額差異 | 2005-2009年の伝票 | 10 | ヘッダー金額と明細合計の差額を原値のまま保存し、採用する会計値を承認する。`36989`は負数例外と重複する。 |
| 酒税区分不一致 | ヘッダーの酒税未納・輸出フラグと酒税区分の不一致 | 686 | 酒税区分を自動で上書きせず、両方の原値を保持して税務判断する。 |

金額差異10件の伝票番号は、`36989`、`40023`、`40521`、`43429`、`43701`、`43997`、`45505`、`45589`、`48767`、`49529`である。これらはすべてB1の履歴値であり、差額を自動補正してはならない。

## 判定と次の条件

バッチ40は `ready_with_warnings` であり、抽出・ステージング・参照整合性検証は完了した。業務データの移行、ロット在庫の作成、旧方式の残高反映、B2への反映は未実施である。

G3を完了とするには、次を満たす必要がある。

1. 一次分類済みの警告2,136件を伝票単位で確認し、除外・修正・移行の扱いと承認者を記録する。特に、返品候補1,369件、例外伝票`36989`、酒税区分不一致686件、将来日付5件を承認対象とする。
2. B1とAで、承認済み対象の伝票件数、数量、金額、酒税、開始在庫を照合する。
3. ロット対応不能または単位不一致のデータを隔離し、ロット・保管場所・単位が確定したものだけを別途承認して取り込む。
4. 業務テーブルへの取込前に、対象範囲とロールバック手順を提示して明示承認を得る。

## A再構築ドライラン（2026-09-07、実行取消）

バッチ40に対して次のドライランを実行した。`--commit`は指定していないため、AのDBには削除、投入、更新を行っていない。

```powershell
docker compose exec -T app php artisan aikura:access-cutover-initialize 40 `
  --cutover=2026-07-01 `
  --opening-date=2026-06-30 `
  --stock-as-of=2026-09-06
```

| 項目 | 値 |
| --- | --- |
| バッチID | 40 |
| B1原本 SHA-256 | `B18CA5BD31EB6A8EF64DD5DE658B605B0BE953F66A516329A00E51BFDBB33D5C` |
| バッチ状態 | `ready_with_warnings` |
| エラー | 0件 |
| 警告 | 2,136件 |
| 取引開始日 | 2026-07-01 |
| 開始売掛日 | 2026-06-30 |
| 在庫基準日 | 2026-09-06 |
| Aの削除予定行数 | 6,237件 |

本実行では、出荷、請求、入金、在庫、ロット、月次残高、税務ドラフトを含むAの業務データが初期化される。B1とB2には接続・書込みを行わない。

本実行の前提は、AのPostgreSQLバックアップの取得・SHA-256・復元手順、警告2,136件の取扱い承認、上記日付と削除予定6,237件の承認である。バックアップを取得しても、利用者が対象を明示承認するまでは`--commit`を実行しない。

その後、Aで月次処理を継続する方針が確定したため、この全量再構築案は採用しない。ドライランは記録として残すが、`--commit`は実行禁止とする。以後は、初回基準バッチを確定した後、B1差分を `Plan -> CSV確認 -> Apply` の順でAへ取り込む。月次確定・締め済み期間へ影響する差分は、自動適用せず個別承認とする。

## 差分基準の読取確認（2026-09-07）

読取専用でAの移行バッチを確認した。A、B1、B2に書込みは行っていない。

| バッチ | 状態 | 判定 |
| ---: | --- | --- |
| 6 | `completed` | 2026-08-17に初回取込、43項目の照合成功。B1-A差分の有効な基準バッチ。 |
| 39 | `ready_with_warnings` | バッチ6との差分計画は作成済み、適用は未実行。差分は`changed` 183件、`new` 0件、`deleted` 0件、自動適用停止対象0件。 |
| 40 | `ready_with_warnings` | 差分計画・適用とも未実行。B1原本SHA-256は `B18CA5BD31EB6A8EF64DD5DE658B605B0BE953F66A516329A00E51BFDBB33D5C`。 |

次の実行候補は、バッチ39の差分CSVを確認してAへの適用可否を承認し、適用後にバッチ40をバッチ39と比較して差分計画を作成することである。バッチ40をバッチ6へ直接重ねて適用してはならない。Aの月次確定・締め済み期間に影響する差分は、適用前に個別承認を要する。

バッチ39の読取集計では、差分183件はすべて `取引先別価格-商品` の `changed` である。取引、在庫、ロット、入金の `new` / `changed` / `deleted` は0件であり、月次確定済み期間への影響はない。Aへの適用は未実行である。

## バッチ39適用前のAバックアップ（2026-09-07）

- 対象: AのローカルPostgreSQLデータベース `aikura`。
- 方式: 稼働停止なしのPostgreSQLカスタム形式ダンプ（`pg_dump -Fc --no-owner --no-privileges`）。
- 保存先: `storage/app/backups/aikura-before-access-delta-batch39-20260907-211656.dump`。
- サイズ: `251,967,040` bytes。
- SHA-256: `0A330D61B57B103F9FCF25226C2BF661568535A85B37239D61DA108DB12AD162`。
- 検証: `pg_restore -l` によりアーカイブ構造の読取を成功確認した。業務DBへの復元は未実行。

このバックアップ取得でAの業務データは更新・削除していない。B1・B2への接続・操作も行っていない。差分適用前には、Aの業務DB `aikura` を変更しない一時DBへ復元し、復元手順を確認する。

### 復元検証

- 一時DB: `aikura_restore_verify_20260907`。
- 実行: 上記ダンプを一時DBだけへ復元し、元DB `aikura` と公開テーブル数を照合した。
- 結果: 元DB `101` テーブル、一時DB `101` テーブルで一致。復元成功。
- 後処理: 一時DBを削除し、残存しないことを確認した。

この検証ではAの業務DB `aikura`、B1、B2を更新・削除していない。

## バッチ39差分適用（2026-09-07）

- 承認済みのバッチ39を、比較元バッチ6に対する差分としてAのローカルDB `aikura` へ適用した。
- 完了状態: `delta_applied`。適用日時: `2026-09-07T21:46:28Z`。
- 適用: `changed` 183件、すべて `取引先別価格-商品`。不変455,104件は `skipped`。`new` / `deleted` は0件。
- 出荷、出荷明細、販売外在庫、入金台帳、入金の取込件数はすべて0件。開始在庫・開始売掛残高は再計算していない。
- 実行環境の時間制限で価格同期後に一度終了したため、価格同期完了を `prices_imported` として永続化し、価格だけの差分では取引取込をスキップして再開できるよう実装を修正した。隔離テストDBで差分移行テスト4件・13アサーションが成功した後、同じバッチを再開して完了した。
- B1・B2への接続、書込み、設定変更は行っていない。

## バッチ40差分計画と適用停止（2026-09-07）

- 比較元は適用済みバッチ39。計画は `ready_with_warnings` のまま作成済みで、A業務テーブルへの適用は未実行である。
- 初回計画ではJSON項目順の差異を内容変更として扱い、`changed` 435,463件の誤判定が発生した。JSONB正規化表現を使う比較へ修正し、隔離テストDBで5テスト・15アサーションの成功後に計画を作り直した。
- 正規化後の計画: `new` 739件、`changed` 920件、`unchanged` 454,317件、`deleted` 50件、自動適用停止対象57件。
- 新規業務データは、出荷ヘッダー141件、出荷明細535件、入金61件、販売外在庫出入1件。日付は2026-08-05から2026-09-07である。

### 自動適用停止対象

- 入金変更1件: 入金ID `20551`、2026-07-31。摘要変更であるが、2026年7月の月次影響を確認する必要がある。
- 出荷変更3件: 伝票`88729`（2024年、金額変更）、`93249`（2026-07-02、金額変更）、`93485`（2026-08-18から2026-08-20への日付変更と明細摘要等の変更）。
- 出荷明細変更3件: 明細`312757`、`312982`、`312983`。
- 出荷明細削除50件: 伝票`88729` 1件、`91110` 11件、`92397` 18件、`93249` 4件、`93309` 11件、`93321` 3件、`93395` 2件。
- Aの2026年7月ロット在庫月次は169件が`confirmed`である。したがって、7月31日の入金訂正と7月2日の出荷訂正は、月次解除・訂正・再集計の個別承認なしに適用してはならない。

バッチ40は、57件の処理方針と承認が確定するまで `aikura:access-delta-apply 40` を実行禁止とする。B1・B2への接続・書込みは行っていない。
