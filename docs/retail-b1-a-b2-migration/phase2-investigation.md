# 工程② 詳細調査結果（2026-09-23）

**工程②完了（玉川小売のみ）。2026-09-23の利用者指示により、桜浜酒店は今回の対象外として別途対応する。工程③の実装、移行・削除・設定変更は未実行。**

今回のB1は `Tamagawa-xp.accdb`、対象会社は `maru` / ID 1。以下のItaro・桜浜の実測結果は調査当時の証跡として残すが、桜浜の取込・設定変更・会計原本確認は今回の工程に含めない。Itaro・酒蔵側は玉川の商品対応・旧連携先・期間・既存取引を確認する参照資料としてのみ用いる。

本書を工程②の最新結果とする。以前の準備文書にある「未確認」は、本書の実測結果で更新する。調査完了は移行実行可・照合合格・本番受入を意味しない。

## 1. 調査方法と証跡

- A/B2のLaravel実効接続から、PostgreSQLの `REPEATABLE READ READ ONLY` トランザクションでカタログ・件数・会社設定・旧ID対応を取得し、最後に `ROLLBACK`。移行コマンドは実行していない。
- B2は `ssh kura`、ホスト名 `X99`。原本AccessはこのPCにあるファイルの調査用コピーをDAOで読取専用オープンし、TableDefs・QueryDefs・Relations・オブジェクト名と限定SELECTを調査。フォーム・レポート・AUTOEXEC・更新クエリは実行していない。
- 調査時のTamagawa原本SHA-256: `3BD175729332F94DF5C3E19C6C5EBEB3A0B60E2073E31037F41EA8A1CA93D537`。保存済み2026-09-22パッケージと一致。
- 調査時のItaro原本SHA-256: `DE9B08B7F0577C1C6FC488DCB65D991C32FFA5FF6AD3F326ABF94E70700C3162`。A/B2の最新バッチ41・42の原本 `CF840548EDFC65649DA4F42DF1BCD5A404AD1925659E895BE90BB16692555DED` より新しい。
- 既存抽出物はTamagawa 30ファイル・Itaro 45ファイルのSHA-256を検査し、いずれもmanifestとの不一致0件。
- 生の証跡・読取スクリプト・証跡ハッシュ一覧: `storage/app/retail-migration-investigation/20260923-phase2/`。Git対象外。Access調査用コピーは `tmp/retail-stage2/` に置き、通常のアプリ配布へ含めない。

## 2. A/B2の実構成

| 項目 | A | B2 |
| --- | --- | --- |
| 所在 | このPCのDocker | `ssh kura` 接続先のX99内Docker |
| アプリ配置 | 本リポジトリを `/var/www/html` へマウント | `C:\docker\酒蔵販売業務システム` を `/var/www/html` へマウント |
| 稼働コンテナ | aikura_app / aikura_nginx / aikura_scheduler / aikura_postgres / aikura_redis | 同名の5コンテナ |
| Web | 8080番、`/retail` | 8080番、`/retail` |
| 小売接続名 | retail | retail |
| 通常接続名 | pgsql | pgsql |
| 実効DB | host=postgres、DB=aikura、schema=public | host=postgres、DB=aikura、schema=public |
| 小売と酒蔵 | 同じDB・アプリ・Web・Scheduler・認証を共有 | 同じDB・アプリ・Web・Scheduler・認証を共有 |
| 小売テーブル | 23 | 24 |

両ホストでDocker内のサーバーアドレスが同じ値でも、AとB2が同じ実DBという意味ではない。各ホスト内で `pgsql` と `retail` が同じDBを指すことを確認した。

### 会社・既存データ

A/B2で一致した会社対応:

| 会社 | retail_companies.id | company_key | 状態 |
| --- | --- | --- | --- |
| 玉川小売 | 1 | maru | 有効 |
| 桜浜酒店 | 2 | batsu | 有効 |
| ▲社 | 3 | sankaku | 無効 |

両環境とも小売顧客6,590件、仕入先1件、会社設定2件、価格同期設定1件、システム設定1件。商品・販売・請求・入金・発注・在庫・在庫移動は0件。これらは小売全体の調査時件数であり、玉川のみの置換件数ではない。既存データ保全不要の扱いは今回の玉川専用範囲に限り、桜浜・他社・共有酒蔵データ・ユーザー・証跡は保持する。

両社とも `inventory_sales_policy=strict_stock`。在庫非管理には未対応。蔵仕入先の `brewery_partner_id` は未設定で、現状のままでは通常販売の酒蔵連携が完結しない。

### スキーマ・コードの差

B2には `2026_09_21_010000_add_retail_purchase_order_cancellation_and_document_sequences` が適用済み、Aは未適用。差は `retail_document_sequences` と発注取消関連6列。今回適用はしていない。工程③でAのスキーマをそろえ、B2の状態を巻き戻さない成果物を固定する。

主要4ファイル（販売作成・採番・酒蔵販売連携・webルート）はCRLF/LFを正規化したSHA-256で一致した。全コードの版同一性を保証するものではなく、工程⑦で配布コミットを固定する。

## 3. 原本・会社・旧ID対応

| 対象 | 元データと抽出条件 | A/B2側の対応 | 取込方針 |
| --- | --- | --- | --- |
| 玉川小売 | Tamagawaの個人顧客・出荷ヘッダー・明細 | company_key=maru / 会社ID 1 | Tamagawa専用マッピング |
| 桜浜酒店（今回対象外） | Itaro `取引先マスター.取引先ID=29`、名称「有光 櫻浜酒店」 | company_key=batsu / 会社ID 2。酒蔵customers.id=55、legacy_code=29 | 今回は取込・再構築しない。調査結果を次回へ引き継ぐ |
| 玉川の旧蔵連携先 | Tamagawa設定 `ItaroSysCode=33` | 酒蔵customers.id=56、legacy_code=33 | 通常連携先候補。会社ごとの仕入先・連携先解決を実装 |
| 玉川小売/A | Itaro取引先ID=34 | 酒蔵customers.id=57 | ID33や桜浜へ統合しない。7月以降の原本取引は0件 |

現行 `RetailBrewerySaleSyncService` は有効な蔵仕入先をID順で1件選ぶ。玉川のみを旧ID33へ対応させ、桜浜・他社の連携設定を変更しない会社別解決を工程③で設計する。共有仕入先の一括変更で代用しない。

### 桜浜酒店の既存取込との照合（次回用の調査記録・今回対象外）

- 2026-09-21保存パッケージ: 桜浜全期間799伝票・1,354明細、7月以降26伝票・39明細、合計73,202円。
- A/B2の7月以降: 同じ26伝票が存在し、旧伝票番号集合の不足・余分0件。AとB2の対象ID・日付・保存合計の差0件。新たに販売連携を呼んで既存酒蔵取引を再作成しない。
- 調査時の現Itaroコピー: 全期間800伝票、7月以降27伝票、合計75,882円。保存パッケージとの差は1伝票・2,680円、最終日は9月22日。別途最終差分として扱い、現在のA/B2へ今回反映しない。
- A/B2には桜浜の6月分を含め31出荷が存在する。出荷指示参照は0件で、既存出荷IDと新しい受注IDを同一視できない。小売専用の移行対応表に既存出荷・旧明細IDを記録する設計とする。

## 4. Tamagawaの業務データ構造

DAO調査で実テーブル30、保存クエリ19、リレーション5、リンクテーブル0を確認した。独立した請求・入金・売掛・発注伝票テーブルはなく、以下の明細・クエリで表現される。

| 業務 | 確認できた構造 | 工程③への要件 |
| --- | --- | --- |
| 顧客 | 個人顧客IDでヘッダーと結合。原本6,605件、氏名・カナともない顧客1件 | 会社1へ対応。氏名なしの扱いを明示し、無言で作成・除外しない |
| 商品・価格 | 商品611件のうち610件はItaroにも同じ商品IDが存在。残る1件は入金用のID=-1。商品に税込希望小売価格、取引明細に単価 | ID一致は候補として商品・単位を照合。過去単価を現価格へ置換しない。外部商品の有無を「小売独自商品」フラグだけで決めない |
| 売上 | ヘッダー: 伝票番号・年月日・個人顧客ID。明細: ID・商品ID・商品詳細ID・個数・単価 | 元明細IDを一意キーにし、金額は保存数量×単価を保持 |
| 入金 | 設定の入金コード=-1、同商品の入金=true・小売独自商品=true。商品明細に入金を記録 | 商品販売へ混ぜず入金系へ分ける。7月以降14明細は全て負額。履歴中には正額もあるので一括絶対値変換をしない |
| 請求・売掛 | `残高一覧表` は商品ID≠-1の数量×単価を請求金額、ID=-1を入金金額、両者の合計を残高にする。`残高一覧表1` は指定日以下を顧客別集計 | 残高は再現可能。独立した請求確定番号・確定日・消込先は保存されていないため捏造しない。開始残高・移行台帳として保存する設計 |
| 返品・値引等 | ヘッダーの戻入取引と数量・単価の符号を保持。負額の通常明細も存在 | 負額を全て返品と判定しない。二重符号反転なし |
| 蔵発注 | `伊太郎システム発注` は変更データ年月日と同日の販売を抽出し、入金コードを除外、設定の取引先ID=33として送る | 販売からの蔵連携として移行設計。発注先商品データ380件を注文伝票にしない |
| 発注状態 | `伊太郎システム発注完了` はチェック済み変更日を削除する定義。今回実行していない。変更データ年月日は0行 | 未送信候補0件。完了履歴が独立保存されないため全発注履歴を復元済みと扱わない。酒蔵側の取引との照合証跡を残す |

販売と入金が同じ伝票に入る。7月以降137ヘッダーの内訳は売上のみ123、入金のみ10、混在4。売上309明細の保存金額合計1,390,209円、入金14明細の符号付き合計-511,222円。これらは元データの集計値であり、税抜／税込や確定請求額の最終判定ではない。

6月末までの残高クエリ相当の集計候補は120顧客・5,378,898.40円。全明細集計と、元クエリ同様に存在する顧客だけを結合した集計の合計は一致した。開始売掛の確定値・承認済み額ではない。

原本の設定税率は5で、明細に取引時税率・保存税額がない。設定値を現在または全履歴へ適用しない。元帳金額と取引時の税内訳を分離し、税内訳が不明な履歴は未確定を保持する。帳票・VBA内の税計算を今回実行して確認したわけではない。

### 参照切れ等

明細の顧客参照切れ71件、商品参照切れ2件（商品ID 5093）は全て7月より前。ヘッダー参照切れ・明細ID重複0件、7月以降の顧客・商品参照切れ0件。過去残高や参照履歴から無断で削除せず、原値・理由を隔離記録する。

## 5. 酒蔵側とそろえる期間

期間は「運用境界」と「履歴の保持範囲」を分ける。

- 既存記録の業務境界は2026-07-01。Aの開始残高106件は `as_of_date=2026-07-01` として保存されており、6月末残高を7月開始残高として表現しているかを、移行仕様に明記する。
- 現在のA/B2のAccess由来出荷は2026-06-01〜2026-09-24、668件。入金は2026-06-01〜2026-09-08、200件。7月以降だけが保存されている状態ではない。
- 最新バッチ41・42の保存された `cutover_date` はnull。バッチ40は2026-07-01。古い記録だけで6月の保存履歴を切り捨てない。
- 小売も6月分を含む参照履歴と7月の開始残高を区別する設計とし、開始残高へ含めた過去分を運用売掛へ二重加算しない。新しい「何年分」という別設定は作らない。最終対象一覧と取引締切は工程③〜④で固定する。

## 6. 小売だけを復元する範囲

現構成では**同じDB内の小売テーブルを論理的に分離した完全復元**を設計する。別DBへの構成変更は今回実施していない。

以下の24テーブルと所有シーケンスは、小売の構造・依存を調べる一覧であり、全行を置換する許可一覧ではない。今回の完全復元単位は玉川小売に限定する。会社列がある表は会社ID 1、子表は玉川の親IDから対象行を決める。共通マスター・共有設定・採番は会社列の有無だけで決めず、他社参照を調べ、対象行・非更新条件・ID対応を工程③で固定する。共有シーケンスをAの値へ丸ごと巻き戻さない。Aの未適用migrationも工程③で整合させる。在庫は投入・削除・ゼロ化せず、不変を確認する。

```text
retail_adjustment_events
retail_brewery_product_import_selections
retail_companies
retail_company_settings
retail_customers
retail_deliveries
retail_delivery_lines
retail_document_sequences
retail_inventory_movements
retail_inventory_stocks
retail_invoice_lines
retail_invoices
retail_payment_allocations
retail_payments
retail_price_change_candidates
retail_price_histories
retail_price_sync_settings
retail_products
retail_purchase_order_lines
retail_purchase_orders
retail_sale_items
retail_sales
retail_suppliers
retail_system_settings
```

カタログ確認では小売に関係する外部キー28本は全て小売内で完結。小売テーブル上の非内部トリガー・参照ビューは今回の照会で0件。ただし `brewery_product_id`、`brewery_partner_id`、酒蔵受注・明細・訂正IDはDB外部キーで保護されていないため、A/B2の論理参照照合が必要。

### 復元対象外と共有資源

- 酒蔵のcustomers・products・sales_orders・shipment_*・invoice_*・payments・ロット在庫・月次、および既存Accessステージング／対応表は置換しない。
- users・認証権限・監査・共有 `migrations` テーブルは丸ごと復元しない。小売migrationの適用状況だけを専用手順で確認する。
- 現小売の納品・請求はBlade表示。調査した小売Controller/Serviceには添付ファイル保存処理を確認していない。共通 `storage/app` の全量上書きを小売復元へ含めない。将来ファイルが増えた場合は小売所有ファイルを個別列挙する。
- app・Web・Scheduler・Redisセッション・認証は共有。独立した小売コンテナを停止する手順は存在しない。小売の書込み経路と毎分の商品検出を止める手段、共有コード配布時の酒蔵影響を工程③で設計する。共有Redisを全消去しない。

### 工程③で作る復元手順の条件

固定した玉川の対象行・依存順・採番条件・帳票・コードの版を一組として扱う。玉川の確定範囲を完全復元し、桜浜・他社・共有行を保持する。小売全表の全行置換、DB全体の `clean`、対象外へ及ぶ `CASCADE` は使用しない。自己参照（元販売・元入金）を含む外部キー順序、途中失敗、採番衝突防止、対象外行の不変、A/B2参照IDの一致を隔離環境で確認する。共有行の競合は自動上書きせず、工程③で解決方法を具体化する。小売専用移行証跡テーブルを追加した場合は対象一覧も版管理する。

## 7. 工程③への引継ぎと判定

| 項目 | 工程②で確定した事実 | 工程③の対応 |
| --- | --- | --- |
| 環境・会社 | A/B2の共有DB構成、会社ID・キーを実測 | 小売の分離復元と共有サービス制御 |
| スキーマ | Aだけ小売migration1件未適用 | A/B2のスキーマを整合させる |
| 在庫 | 玉川strict_stock、非管理未実装 | 玉川の販売・返品・訂正・発注提案へ非管理モードを反映。他社設定は保持 |
| 連携 | 仕入先の蔵取引先ID未設定、現コードは会社別解決なし | 玉川=旧33の会社別対応と履歴再送抑止。他社の連携は変更しない |
| 原本変換 | 売上・入金混在、残高は集計値、確定請求・消込履歴なし | 専用取込・開始残高・未確定状態・旧ID追跡を実装 |
| 例外 | 過去の参照切れ、符号付き入金、税内訳欠落 | 原値を保全し、推測で請求・消込・税額を作らない |
| 対象外保全 | 桜浜・他社は同じ小売テーブルを使用 | 玉川の親子行と共通マスターを区別し、桜浜・他社・共有行の不変を検証 |

**対象変更による完了判定:** 利用者が今回の対象を玉川小売に限定したため、桜浜の会計原本についての確認依頼は今回分から取り下げる。桜浜の会計原本が確定したという意味ではなく、桜浜の移行再開時に確認する。

玉川小売を対象とする工程②の調査・課題整理は完了。玉川単位の復元処理、税・開始残高等の変換仕様、在庫非管理は工程③の設計・実装課題として引き継ぐ。確認体制は依然未設置であり、実行段階までに整える。工程③・④以降の実行指示を受けたものとは扱わない。
